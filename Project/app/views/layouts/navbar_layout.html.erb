<!DOCTYPE html>
<html>
<%= stylesheet_link_tag 'profile', media: 'all' %>

   <head>
       <nav class="top-nav">
        <div style="width: 25%; text-align: left;">
          <%= link_to "GameSalesTracker", root_path, class: "button-link" %><br><br>
          <%= link_to "My Wishlist", support_path, class: "button-link" %>
        </div>
        <div style="width: 50%; text-align: center;">
          <% if user_signed_in? %>
            Welcome back, <%= current_user.email%>! [<%= link_to 'Logout', destroy_user_session_path, :method => :delete%>]
          <% else %>
        <!-- Content for non-logged-in users -->
            New here? <%= link_to "Sign Up", new_user_registration_path, class: "button-login" %>
          <% end %>
          <br><br>
      <%= form_with url: search_games_path, method: :get do |f| %>
        <%= f.label :game_name, "Nome del gioco" %>
        <%= f.text_field :game_name %>
        <%= f.submit "Cerca" %>
      <% end %>
      </div>
      <div style="width: 25%; text-align: right;">
      <% if user_signed_in? %>
        <%= link_to "My account", profile_profile_page_path, class: "button-link" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "button-link" %>
      <% end %>
      <br><br>
        <%= link_to "Support", support_path, class: "button-link" %>
      </div>
    </nav>
   <% flash.each do |type, msg| %> <div>
    Notification: <%= msg %>
  </div>
<% end %>
    <title>GameSalesTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  </body>
  <br><br>
</html>

<%= link_to "Need help?", support_path %>
