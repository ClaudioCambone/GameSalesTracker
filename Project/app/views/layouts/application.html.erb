<!DOCTYPE html>
<html>
   <head>
   <% flash.each do |type, msg| %> <div>
    Notification: <%= msg %>
  </div>
<% end %>
   <%= link_to "Back to Main Page", root_path %>
    <title>Project</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  </body>
  <br><br>
</html>
<nav>

<%= link_to "Need help?", support_path %>

<% if user_signed_in? %>
Logged in as <%= current_user.email%> [<%= link_to 'Logout', destroy_user_session_path, :method => :delete%>]
<% else %>
  <!-- Content for non-logged-in users -->
  <%= link_to "Login", new_user_session_path %>
  <%= link_to "Sign Up", new_user_registration_path, class: "button-sign-up" %>
  <!-- Add any other content specific to non-logged-in users here -->
<% end %>
