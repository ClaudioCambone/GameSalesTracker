<% if current_user %>
  <h2>Create a Collection</h2>

  <%= form_with(model: Collection.new, url: collections_path, method: :post) do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>

    <%= form.submit 'Create Collection' %>
  <% end %>
<% end %>
