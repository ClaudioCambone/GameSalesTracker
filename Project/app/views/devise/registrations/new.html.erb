<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css">
  <%= stylesheet_link_tag 'application', media: 'all' %>
</head>
<body>

<section class="vh-100">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card bg-dark text-white" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">
            <div class="mb-md-0 mt-md-0 pb-0">
              <h2 class="fw-bold mb-2 text-uppercase">Sign up</h2>
              <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "form" }) do |f| %>
                <!-- Avatar Upload Field -->
                <div class="mb-3">
                  <div class="input-group d-flex">
                    <label class="input-group-text" for="avatarInput" style="border-radius: 1rem;">
                      Upload an avatar:&nbsp;<i class="fas fa-upload"></i>
                    </label>
                    <%= f.file_field :avatar, class: "form-control visually-hidden", id: "avatarInput" %>
                  </div>
                </div>
                <!-- Avatar Upload Field -->

                <div class="form-outline form-white mb-4">
                  <%= f.text_field :username, class: "form-control form-control-lg", id: "typeUsername", autofocus: true %>
                  <label class="form-label" for="typeUsername">Username</label>
                </div>

                <div class="form-outline form-white mb-4">
                  <%= f.email_field :email, class: "form-control form-control-lg", id: "typeEmail", autocomplete: "email" %>
                  <label class="form-label" for="typeEmail">Email</label>
                </div>

                <div class="form-outline form-white mb-4">
                  <%= f.password_field :password, class: "form-control form-control-lg", id: "typePassword", autocomplete: "new-password" %>
                  <label class="form-label" for="typePassword">Password</label>
                </div>

                <div class="form-outline form-white mb-4">
                  <%= f.password_field :password_confirmation, class: "form-control form-control-lg", id: "typePasswordConfirmation", autocomplete: "new-password" %>
                  <label class="form-label" for="typePasswordConfirmation">Confirm Password</label>
                </div>

                <%= f.submit "Sign up", class: "btn btn-outline-light btn-lg px-5" %>
              <% end %>
            </div>
            <!-- Social media icons here -->
          </div>
          <div class="text-center">
            <%= render "devise/shared/links" %>
          </div>
          <div class="mt-3 mb-3 text-center">
            <%= link_to "Go back to homepage", root_path, class: "text-white-50" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

</body>
</html>
